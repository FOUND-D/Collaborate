{
  "name": "com.collaborate.app",
  "displayName": "Collaborate",
  "version": "1.0.0",
  "description": "Cordova shell for the Collaborate React app",
  "main": "index.js",
  "scripts": {
    "sync": "npm run build:cordova --prefix ../client",
    "prepare:android": "npm run sync && cordova prepare android",
    "android": "npm run sync && cordova run android",
    "build:android": "npm run sync && cordova build android",
    "build:android:debug": "npm run sync && cordova build android --debug"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Collaborate Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova": "^12.0.0",
    "cordova-android": "^15.1.0"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}